- name: add fish repo
  become: yes
  apt_repository:
    repo:         'ppa:fish-shell/release-2'
    state:        present
    update_cache: yes

- name: install fish
  become: yes
  apt:
    name:         'fish'
    state:        present
    update_cache: yes

